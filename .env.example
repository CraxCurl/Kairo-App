# ===================================================
# Kairo Mobile Companion App — Environment Configuration
# ===================================================

# MongoDB Connection URI (Optional: Provide your MongoDB Atlas or local URI)
# Example: MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/kairo?retryWrites=true&w=majority
MONGODB_URI=

# Custom Backend API Endpoint (Optional: defaults to /api)
VITE_API_URL=
